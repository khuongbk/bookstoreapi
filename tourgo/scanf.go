package main 

import "os"
// import "fmt"
func main(){
   panic("a problem")
    _,err :=os.Create("tmp/file")
        if err !=nil {
        panic(err
            // fmt.Printf("you have errs")
        }
}